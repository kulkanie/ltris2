von lbrhd:
- mainwindow mit flickerschutz
- tasten für menüsteuerung
- confirmation keys mit gettext

- pot datei generieren?
- config überarbeiten
- SDL_CONFIG setzen

VGame 
- state
- update() mit controls und ms
- translate controls for bowl.c
- render() renders bowls
- start copies vconfig to config for bowl.c

VBowl (?)
- update() übersetzt SDL_events/states in controls
- render() rendert bowl (+ player info?)

single player: 
bowl centered, preview attached right, hold attached left, score below
blocksize = screenheight / 25

Changes to make ltris1 core work:
- disable all SDL.h includes, except in sdl.h: change SDL.h include to SDL2.h
- define compat macros:
	#define SDLK_LAST SDL_NUM_SCANCODES
	#define SDL_FULLSCREEN 0
	#define SDL_SRCCOLORKEY 0
	#define SDL_NOEVENT 0
	#define SDL_GetKeyState SDL_GetKeyboardState
- empty all functions in sdl.c but create dummy surfaces and dummy font to
  allow access of w and h attributes
- rename Font to OldFont
- turn ALIGN_X_LEFT,... into macros and only set if not yet defined 
- disable bowl_quick_game() in bowls.h
